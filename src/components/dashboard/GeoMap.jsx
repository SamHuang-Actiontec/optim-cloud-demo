const GeoMap = ({ onRegionSelect }) => {
  return (
    <div style={{
      width: '100%',
      height: '100%',
      background: '#e0e0e0',
      borderRadius: '12px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontSize: '14px',
      color: '#666',
    }}>
      Regional Map (Coming Soon)
    </div>
  );
};

export default GeoMap;
